# PR2: User Profile & Onboarding Flow

**Status:** Implemented  
**Source of truth:** [`docs/technical-spec.md`](../technical-spec.md) (PR 2 section), [`docs/engineering-rules.md`](../engineering-rules.md), [`docs/product-research.md`](../product-research.md), [`PR-01.md`](./PR-01.md), [`README.md`](../../README.md)

---

## 1. Objective

Deliver the first-run onboarding experience: collect one or two user profiles, calculate calorie and macro targets via `NutritionCalculator`, request HealthKit authorization, optionally validate and store API keys in Keychain, and persist `UserProfile` records to SwiftData. After completion, `RootView` gates to a minimal `DashboardView` stub; relaunch skips onboarding when profiles exist.

---

## 2. In scope

- **Launch gating** — `RootView` uses `@Query` on `UserProfile`; empty → `OnboardingView`, else → `DashboardView`
- **Seven-step onboarding flow** — Welcome, Profile Setup, Goal Setup, Calorie Target Preview, HealthKit, API Keys, Done
- **Dual-user loop** — Steps 2–4 repeat for partner when partner name is non-empty on Welcome
- **`OnboardingViewModel`** — Step navigation, validation, target calculation, HealthKit request, API key save, profile persistence
- **`ProfileDraft`** — In-memory draft struct; converted to `UserProfile` via repository
- **`UserProfileRepository`** — `makeUserProfile(from:)`, batch `save(_:context:)`, `fetchAll`
- **`AppContainer` wiring** — Concrete `HealthKitService`, `GeminiService`, `UserProfileRepository`; injected via `.environment(appContainer)`
- **`HealthKitService`** — Authorization only (read body mass/height; write dietary + body mass)
- **`GeminiService`** — `validateAPIKey(_:)` only (trivial `generateContent` call); **Gemini key skippable** on Continue
- **`APIKeyResolver`** — Keychain-first Gemini/USDA resolution; USDA falls back to `AppConstants.USDA.demoAPIKey`
- **`UnitFormatters`** — kg/lbs and cm/ft-in conversion for onboarding pickers
- **`UserProfile+Display`** — Display helpers for imperial units
- **`AppConstants.Onboarding`** — Age 18–90; goal date 14–730 days from today
- **`AppStorageKey.activeUserId`** — Shared key for default active profile after onboarding
- **HealthKit entitlements + Info.plist usage strings**
- **SPM:** `generative-ai-swift` (`GoogleGenerativeAI`) with `Package.resolved` committed
- **Unit tests:** exactly three onboarding cases per spec
- **Post-review fixes:** atomic batch profile save; `.id(currentProfileIndex)` on profile/goal steps; README xcodegen warning; `OnboardingViewModelTests` unwraps profile before `accuracy` assertions
- **Skills alignment:** per-profile unit toggles on `ProfileDraft`; `NavigationStack` wrapper; extracted `OnboardingStepContent`, `OnboardingNavigationBar`, `GeminiTestIndicatorView`, `GeminiTestState`; VM binding helpers; profiles-before-keys save order; HealthKit error surfacing; `AppConstants.Nutrition` macro defaults

---

## 3. Out of scope

- Full `DashboardView` (calorie ring, meals, FAB) — PR3
- `DashboardViewModel`, meal logging, scanner, USDA meal analysis
- `GeminiService.analyzeMeal` and any vision/meal pipeline
- Settings screen, profile editing after onboarding
- Camera / photo library permissions
- Design system (`Colors`, `Typography`, shared components beyond step layouts)
- UI tests, snapshot tests
- Updating `project.yml` / running `xcodegen generate` (PR2 edits live in `CalSnap.xcodeproj` only)
- Plateau detection UI, weigh-in flow
- PR3+ work of any kind

---

## 4. Files to create

| Path | Purpose |
|------|---------|
| `CalSnap/Features/Onboarding/OnboardingStep.swift` | Seven-step enum with titles |
| `CalSnap/Features/Onboarding/ProfileDraft.swift` | Draft model for onboarding form state |
| `CalSnap/Features/Onboarding/OnboardingViewModel.swift` | `@Observable` VM: navigation, validation, calc, persistence |
| `CalSnap/Features/Onboarding/OnboardingView.swift` | Container: progress bar, step switch, back/continue |
| `CalSnap/Features/Onboarding/WelcomeStepView.swift` | App name, tagline, user + optional partner name |
| `CalSnap/Features/Onboarding/ProfileSetupStepView.swift` | Sex, DOB, height/weight with imperial toggles |
| `CalSnap/Features/Onboarding/GoalSetupStepView.swift` | Goal weight, target date, activity level cards |
| `CalSnap/Features/Onboarding/CalorieTargetPreviewStepView.swift` | TDEE, deficit slider, macro summary, science blurb |
| `CalSnap/Features/Onboarding/HealthKitPermissionStepView.swift` | HealthKit explanation + authorize button |
| `CalSnap/Features/Onboarding/APIKeySetupStepView.swift` | Gemini (test + skippable) and optional USDA keys |
| `CalSnap/Features/Onboarding/OnboardingDoneStepView.swift` | Completion message (rarely visible; see §9) |
| `CalSnap/Features/Onboarding/GeminiTestState.swift` | Gemini key test state enum |
| `CalSnap/Features/Onboarding/OnboardingStepContent.swift` | Progress bar, step switch, validation error |
| `CalSnap/Features/Onboarding/OnboardingNavigationBar.swift` | Back / Continue navigation bar |
| `CalSnap/Features/Onboarding/GeminiTestIndicatorView.swift` | Gemini key test result indicator |
| `CalSnap/Features/Dashboard/DashboardView.swift` | Stub: welcome + daily target; `@AppStorage` active user |
| `CalSnap/Core/Repositories/UserProfileRepository.swift` | Draft → model mapping; batch SwiftData save |
| `CalSnap/Core/Services/HealthKitService.swift` | `requestAuthorization()` actor |
| `CalSnap/Core/Services/GeminiService.swift` | `validateAPIKey(_:)` via Google Generative AI SDK |
| `CalSnap/Core/Utilities/APIKeyResolver.swift` | Runtime key resolution (USDA demo fallback) |
| `CalSnap/Core/Utilities/Extensions/UnitFormatters.swift` | Metric/imperial conversion helpers |
| `CalSnap/Core/Models/UserProfile+Display.swift` | Display extensions on persisted profile |
| `CalSnap/Resources/CalSnap.entitlements` | HealthKit capability |
| `CalSnapTests/OnboardingViewModelTests.swift` | Three spec test cases |
| `CalSnap.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved` | Pinned `generative-ai-swift` 0.5.6 + transitive deps |

---

## 5. Files to modify

| Path | Change |
|------|--------|
| `CalSnap/App/RootView.swift` | `@Query` gating: onboarding vs dashboard |
| `CalSnap/App/AppContainer.swift` | Wire `healthKitService`, `geminiService`, `userProfileRepository` |
| `CalSnap/Core/Utilities/Constants.swift` | Add `AppStorageKey`, `AppConstants.Onboarding`, `USDA.demoAPIKey` |
| `CalSnap/Core/Models/Enums.swift` | `ActivityLevel.systemImage` for goal step cards |
| `CalSnap/Resources/Info.plist` | `NSHealthShareUsageDescription`, `NSHealthUpdateUsageDescription` |
| `CalSnap.xcodeproj/project.pbxproj` | New sources, entitlements, SPM `generative-ai-swift` dependency |
| `README.md` | xcodegen warning; `DEVELOPER_DIR` test command |

---

## 6. Implementation steps

1. Add HealthKit entitlement and Info.plist usage strings; link entitlements in Xcode project
2. Extend `Constants.swift` with onboarding bounds, USDA demo key, and `AppStorageKey`
3. Add `UnitFormatters`, `UserProfile+Display`, `APIKeyResolver`
4. Implement `UserProfileRepository.makeUserProfile(from:)` using `NutritionCalculator` (macro fractions 0.28 / 0.47 / 0.25 at model creation)
5. Implement `HealthKitService.requestAuthorization()` and `GeminiService.validateAPIKey(_:)`
6. Add `generative-ai-swift` SPM package to `CalSnap.xcodeproj`; commit `Package.resolved`
7. Wire services into `AppContainer`; inject via `.environment(appContainer)` in onboarding
8. Implement `ProfileDraft`, `OnboardingStep`, `OnboardingViewModel` with dual-user loop (`currentProfileIndex`, computed `activeProfile`)
9. Build seven step views + `OnboardingView` container (`#Preview` on each step view and container)
10. Update `RootView` for `@Query` launch gating; add `DashboardView` stub with `@AppStorage(AppStorageKey.activeUserId)`
11. Add `OnboardingViewModelTests` (three cases)
12. Apply review fixes: batch `save([UserProfile])`, `.id("…-\(currentProfileIndex)")` on profile/goal steps
13. Build & test: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme CalSnap -destination 'platform=iOS Simulator,name=iPhone 16' test`

### Onboarding navigation (single user)

```
welcome → profileSetup → goalSetup → caloriePreview → healthKit → apiKeys → [save] → done
```

### Onboarding navigation (dual user)

After user A's calorie preview, loop back to `profileSetup` with `currentProfileIndex = 1`, then continue through goal + preview before shared HealthKit / API key steps.

### Persistence timing (step 6 Continue)

On API Keys Continue:

1. `saveProfiles(context:)` — batch insert both profiles, single `context.save()`
2. `saveAPIKeys()` — Keychain only for non-empty trimmed inputs (Gemini optional)
3. Set `AppStorageKey.activeUserId` to profile A's UUID
4. Advance to `.done` (RootView immediately shows dashboard via `@Query`)

---

## 7. Tests

### `OnboardingViewModelTests`

| Test | Verifies |
|------|----------|
| `testOnboardingValidation()` | Blank name on profile setup prevents advance; non-blank allows advance |
| `testGoalDateMinimum()` | Goal date 7 days out rejected; 14 days out accepted |
| `testProfilePersistence()` | `ProfileDraft` → `UserProfile` → in-memory SwiftData round-trip; macro defaults 0.28/0.47/0.25; positive TDEE and daily target |

PR1 tests (`NutritionCalculatorTests`, `KeychainManagerTests`) remain unchanged and must still pass.

---

## 8. Acceptance criteria mapping

| Acceptance criterion (technical-spec PR2) | Satisfied by |
|-------------------------------------------|--------------|
| Full onboarding flow navigable on iPhone simulator | Seven step views + `OnboardingView` navigation |
| UserProfile(s) persisted to SwiftData on completion | `saveProfiles` batch save at API Keys Continue |
| HealthKit authorization request fires on step 5 | `HealthKitPermissionStepView` → `requestHealthKit()` |
| Gemini API key stored in Keychain, not UserDefaults | `saveAPIKeys()` via `KeychainManager` |
| App skips onboarding on second launch if profile exists | `RootView` `@Query` gating |

| Additional implemented behavior | Satisfied by |
|---------------------------------|--------------|
| Dual-user setup when partner name provided | Welcome partner field + loop in `advance()` |
| USDA optional; demo key at runtime if omitted | `APIKeyResolver.resolvedUSDAAPIKey()` |
| Gemini key validation without blocking completion | Skippable Continue; test button optional |
| Active profile defaults to user A | `AppStorageKey.activeUserId` set on save |

---

## 9. Risks and edge cases

| Risk | Mitigation |
|------|------------|
| Partial dual-profile save (A saved, B fails) | Batch insert + single `context.save()` in repository |
| Stale `@State` when switching user A ↔ B | `.id("profileSetup-\(currentProfileIndex)")` and same for goal step |
| `xcodegen generate` wipes PR2 project settings | Documented in README; edit `CalSnap.xcodeproj` directly until `project.yml` updated |
| Gemini SDK spec timing (PR4 in original spec) | Approved pull-forward: validate-only in PR2; `analyzeMeal` deferred |
| Step 7 Done rarely visible | `@Query` in `RootView` updates as soon as profiles persist; Done is transient |
| Keys saved but profiles fail | Profiles save first; Keychain write happens only after successful SwiftData save |
| Profiles saved but keys fail | Onboarding completes; `validationError` shows keychain message if user entered keys (RootView may already show dashboard) |
| Xcode license / CI | Local verify with `DEVELOPER_DIR=… xcodebuild test` after accepting license |

### Spec extensions (document in PR description)

1. **`generative-ai-swift` in PR2** — SDK added early for `validateAPIKey` only; meal analysis remains PR4+.
2. **Gemini key skippable** — User can finish onboarding without entering or testing a Gemini key.
3. **`USDA.demoAPIKey`** — `"DEMO_KEY"` constant; Keychain used only when user enters a key; `APIKeyResolver` provides runtime fallback.
4. **`activeProfile`** — Computed get/set over `profileA` / `profileB` via `currentProfileIndex`, not a standalone copied draft.
5. **`UserProfileRepository.save`** — Accepts `[UserProfile]` for atomic multi-profile persistence.
6. **`AppStorageKey`** — Centralized `"activeUserId"` string constant.
7. **Project maintenance** — PR2 SPM, entitlements, and file references edited in `CalSnap.xcodeproj`; `project.yml` not updated in this PR.

---

## 10. Definition of done

- [x] Seven-step onboarding navigable for single and dual-user paths
- [x] `UserProfile` persisted via batch save at end of API Keys step
- [x] HealthKit entitlement + usage strings present
- [x] Gemini key → Keychain when entered; skippable when empty
- [x] USDA key → Keychain when entered; demo fallback via `APIKeyResolver`
- [x] `RootView` skips onboarding when `@Query` returns profiles
- [x] `DashboardView` stub shows active profile name and daily target
- [x] `AppContainer` wires HealthKit, Gemini, repository services
- [x] `generative-ai-swift` added with `Package.resolved` committed
- [x] Exactly three new unit tests in `OnboardingViewModelTests`
- [x] Post-review fixes: atomic save, step `.id()`, `AppStorageKey`, README xcodegen warning
- [x] All unit tests pass on iOS Simulator — verified 2026-06-14 (16 tests)

---

## 11. Suggested commit sequence

1. **`feat: add HealthKit entitlement, Info.plist strings, and onboarding constants`**
2. **`feat: add UserProfileRepository, APIKeyResolver, UnitFormatters, HealthKitService`**
3. **`feat: add GeminiService with generative-ai-swift and Package.resolved`**
4. **`feat: add OnboardingViewModel, ProfileDraft, and step views`**
5. **`feat: wire RootView gating, AppContainer, and Dashboard stub`**
6. **`test: add OnboardingViewModelTests`**
7. **`fix: atomic profile save, step identity reset, AppStorageKey, README xcodegen warning`**

---

## PR description snippet

Use when opening the PR:

> **PR2: User profile & onboarding flow**
>
> Adds first-run onboarding (single or dual user), SwiftData profile persistence, HealthKit authorization, optional Gemini key validation, and launch gating to a dashboard stub.
>
> **Spec extensions:**
> - Pulls in `generative-ai-swift` early for `validateAPIKey` only (meal analysis deferred).
> - Gemini API key is skippable; USDA falls back to `DEMO_KEY` when not entered.
> - Profiles saved atomically in one SwiftData transaction.
> - Project settings maintained in `CalSnap.xcodeproj` (do not run xcodegen until `project.yml` is updated).
>
> **Test plan:**
> - `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme CalSnap -destination 'platform=iOS Simulator,name=iPhone 16' test`
> - Manual: complete single-user onboarding; repeat with partner name; relaunch skips onboarding; tap HealthKit authorize; test Gemini key (optional skip).
