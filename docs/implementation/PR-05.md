# PR5: Meal Detail, Edit & Daily Log

**Status:** Implemented  
**Source of truth:** [`docs/technical-spec.md`](../technical-spec.md) (PR 5 section), [`docs/engineering-rules.md`](../engineering-rules.md), [`PR-04.md`](./PR-04.md), [`PR-03.md`](./PR-03.md)

---

## 1. Objective

Complete the daily log experience: view logged meals, edit via pre-populated scanner, delete with SwiftData removal + HealthKit reversal, dashboard navigation (tap / swipe / context menu), and Daily Summary Footer.

---

## 2. In scope

- **`MealHealthSnapshot`** — value-type pre-delete/pre-edit HK capture
- **`MealRepository`** — `fetchMeal`, `delete` (throws `MealRepositoryError.mealNotFound`), `update` (in-place, replace child items)
- **`HealthKitService.reverseMeal(_:)`** — negative dietary samples
- **`MealDeletionService`** — shared snapshot → delete → HK reverse flow
- **`MealScannerViewModel` edit mode** — `loadForEditing`, `saveMeal`, `updateMeal`
- **`Features/MealLog/`** — `MealDetailView`, `MealListView`, `MealShareCardView`, `DashboardRoute`
- **`DailySummaryFooterView`** — fiber, net kcal, macro % text
- **Typed `NavigationStack(path:)`** — `DashboardRoute` / `MealScannerRoute`
- **Non-scrolling embedded `List`** in dashboard `ScrollView` for swipe actions
- **Unit tests** — `testMealDeletion`, `testMealEdit`

---

## 3. Out of scope

- USDA fallback, Settings API-key UI, privacy manifest (PR8–PR10)
- `WeighInView`, full weight chart (PR6)
- HK sample UUID storage on `MealEntry`
- `project.yml` / xcodegen

---

## 4. Files created

| Path | Purpose |
|------|---------|
| `CalSnap/Core/Models/MealHealthSnapshot.swift` | HK reversal value snapshot |
| `CalSnap/Features/MealLog/DashboardRoute.swift` | Typed navigation routes |
| `CalSnap/Features/MealLog/MealDeletionService.swift` | Shared delete + HK reverse |
| `CalSnap/Features/MealLog/MealDetailView.swift` | Read-only meal + Edit/Share/Delete |
| `CalSnap/Features/MealLog/MealDetailViewModel.swift` | Refresh, delete, share image |
| `CalSnap/Features/MealLog/MealShareCardView.swift` | `ImageRenderer` share card |
| `CalSnap/Features/MealLog/MealListView.swift` | Grouped list, swipe, context menu |
| `CalSnap/Features/Dashboard/DailySummaryFooterView.swift` | Daily summary footer |
| `CalSnapTests/MealLogCRUDTests.swift` | Deletion + edit aggregate tests |

---

## 5. Files modified

| Path | Change |
|------|--------|
| `CalSnap/Core/Repositories/MealRepository.swift` | `fetchMeal`, `delete`, `update` |
| `CalSnap/Core/Services/HealthKitService.swift` | `reverseMeal(MealHealthSnapshot)` |
| `CalSnap/Features/MealScanner/MealScannerViewModel.swift` | Edit mode, `saveMeal`/`updateMeal` |
| `CalSnap/Features/MealScanner/EditableFoodItem.swift` | `from(foodItem:)` |
| `CalSnap/Features/MealScanner/MealScannerView.swift` | `MealScannerRoute`, save routing |
| `CalSnap/Features/MealScanner/MealAnalysisResultView.swift` | `isEditing` UI |
| `CalSnap/Features/MealScanner/FoodItemRowView.swift` | Optional `onEdit` for read-only |
| `CalSnap/Features/Dashboard/DashboardView.swift` | Path navigation, delete alert, footer |
| `CalSnap/Features/Dashboard/DashboardViewModel.swift` | Footer computed props |
| `CalSnap.xcodeproj/project.pbxproj` | Register PR5 sources; remove `TodaysMealsSection` |

**Removed:** `CalSnap/Features/Dashboard/TodaysMealsSection.swift`

---

## 6. Spec extensions

1. **HealthKit reversal** — `MealHealthSnapshot` + negative samples; no HK UUID fields.
2. **Delete flow** — snapshot before `MealRepository.delete`; delete throws if meal already removed.
3. **Edit persistence** — in-place update; preserve `id`/`userId`/`timestamp`; replace `FoodItem` children; HK reverse old + log new.
4. **Fiber target** — `(dailyCalorieTarget / 1000) × fiberGramsPer1000Kcal`; color at 90% / 70%.
5. **Dashboard refresh** — explicit `reloadDashboard()` on path pop / delete.
6. **Navigation** — `DashboardRoute` path with `MealEntry` in cases.
7. **List layout** — non-scrolling `List` inside `ScrollView`.
8. **Empty meal sections** — all four `MealType` sections always visible.
9. **Delete confirmation** — alert for swipe and context-menu delete.

---

## 9. Post-review fixes

| Issue | Fix |
|-------|-----|
| Stale detail after edit-from-detail | `MealDetailView` refreshes on `navigationPath.count` decrease |
| Edit used route-captured `meal` | Edit passes `viewModel.meal` (fresh fetch with items) |
| `updateMeal` silent no-op | Throws `MealScannerError.notInEditMode` when not editing |
| List height clipped photo rows | Per-row height includes `rowPhotoHeightBonus` when `photoData` present |
| Orphan `TodaysMealsSection` pbxproj refs | Removed stale `PBXBuildFile` / `PBXFileReference` entries |
| Silent `delete` on missing meal | `MealRepository.delete` throws `MealRepositoryError.mealNotFound` |

---

## 10. Tests

| Test | Verifies |
|------|----------|
| `testMealDeletion()` | Delete removes meal; dashboard totals update |
| `testMealEdit()` | Edit doubles item weight; totals and persisted meal update |

**Regression:** 21 tests total (19 PR1–PR4 + 2 PR5).

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme CalSnap -destination 'platform=iOS Simulator,name=iPhone 17' test
```

---

## 11. Definition of done

- [x] Meal detail, edit, delete, share
- [x] Meal list navigation, swipe, context menu
- [x] Daily Summary Footer
- [x] `MealHealthSnapshot` delete flow
- [x] Typed `NavigationStack` routing
- [x] `testMealDeletion`, `testMealEdit`
- [x] Post-review fixes applied (detail refresh, edit meal source, delete/update errors, list height, pbxproj cleanup)
- [x] All 21 unit tests pass — verified 2026-06-14
